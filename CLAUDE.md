# CLAUDE.md — niiza-ext（Chrome拡張の配信リポジトリ）

このフォルダは `brt-ext/niiza-ext`（**Public**）のクローン。
中身は CBCM で全社PCに配信されている .crx と update.xml の実体。
ここへの push は即座に本番配信に反映される。

## 作業原則

1. **セッション開始時、まず `git fetch` と `git status` を実行し、
   手元とGitHubの差分を報告してから作業に入ること。**
   報告なしに作業を始めない。
2. **作業が一区切りついたら、commit と push を必ず提案すること。**
   BRTが席を立つ前に手元だけに変更が残っている状態を作らない。
3. **push が拒否された場合、自分で解決を試みず、そのまま報告して止まること。**

## 禁止事項

1. **`git push -f`（強制push）を使わない。** GitHub上の履歴は巻き戻せない。
2. **既存ファイルを削除・改名・移動しない。** 古い .crx が多数並んでいるのは
   意図的な状態。掃除・整理の類は、たとえ不要に見えても必ず指示を待つ。
3. **`.pem` をこのフォルダに置かない。** Publicリポジトリのため、
   秘密鍵の混入は全拡張の署名を第三者に開放することになる。
4. **update.xml は指示された拡張の該当箇所のみ変更する。**
   このファイルは配信中の全拡張が共有している。他の拡張の記述には触れない。
